# Frontend ortam değişkenleri (Vite build / dev)
# Kök env.example ile birlikte kullanın.

# Dev'de boş bırakılabilir — Vite proxy /api isteklerini localhost:3000'e yönlendirir
# VITE_API_BASE=https://senin-api.onrender.com

# Zorunlu: MongoDB ObjectId formatında firma kimliği
VITE_COMPANY_ID=507f1f77bcf86cd799439011

# VITE_USE_API=false ile yalnızca mock veri kullanılır (referans UI)
# VITE_USE_API=true veya boş: API dener, boşsa mock'a düşer
# VITE_USE_API=true
